# Domain layer

Framework-independent business rules belong here. Initial modules should cover reciprocal match eligibility, home availability, travel intents, and exchange lifecycle transitions. UI components and route handlers should call this layer rather than reimplementing those rules.
